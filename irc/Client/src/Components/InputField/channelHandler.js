/* socket.on("new_channel_created", (newChannel) => {
   const channelId = newChannel._id; 

   // Ajoute le channel au state
   setChannelsList((prevChannels) => [...prevChannels, newChannel]); 

   socket.emit("join_channel", channelId);
}); */
